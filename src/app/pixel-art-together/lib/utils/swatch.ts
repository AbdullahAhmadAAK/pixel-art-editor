/**
 * Represents a swatch of colors, where each color is a hex string.
 *
 * Each string in the array must be a valid 6-character or 3-character hex code prefixed with `#`.
 *
 * @example
 * const swatch: Swatch = ["#ff0000", "#00ff00", "#0000ff"];
 */
export type Swatch = string[];
