/**
 * Represents a color in the RGBA (Red, Green, Blue, Alpha) format.
 */
export type RGBA = {
  /** Red component (0-255) */
  r: number;

  /** Green component (0-255) */
  g: number;

  /** Blue component (0-255) */
  b: number;

  /** Alpha (opacity) component (0-1) */
  a: number;
};
