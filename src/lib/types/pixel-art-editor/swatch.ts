/**
 * A Swatch represents an array of color strings.
 * 
 * Each string in the array corresponds to a color, typically in HEX, RGB, HSL, or CSS named format.
 * 
 * @example
 * const exampleSwatch: Swatch = ["#FF5733", "#33FF57", "#3357FF"];
 * 
 * @typedef {string[]} Swatch
 */
export type Swatch = string[];
