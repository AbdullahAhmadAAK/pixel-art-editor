/**
 * A color value for a pixel, represented as a hex string.
 * It should follow one of these formats:
 * - `#RRGGBB` (e.g., `"#ffaa12"`)
 * - `#RRGGBBAA` (e.g., `"#ffaa1021"`, where `AA` represents alpha transparency).
 */
export type PixelColor = string;