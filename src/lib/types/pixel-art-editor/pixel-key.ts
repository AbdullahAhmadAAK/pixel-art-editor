/**
 * A string representing a unique key for a pixel in the format `{layer}_{row}_{col}`.
 * Example: `"0_1_2"` where:
 * - `0` is the layer index.
 * - `1` is the row index.
 * - `2` is the column index.
 */
export type PixelKey = string;