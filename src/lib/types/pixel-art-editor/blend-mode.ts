/**
 * Represents a blend mode option.
 */
export type BlendMode = {
  /** Technical name of the blend mode, used for styling the g element in the PixelGrid component */
  name: string;
  /** User-friendly label for the blend mode */
  label: string;
};