/**
 * Represents a color in the RGB (Red, Green, Blue) format.
 */
export type RGB = {
  /** Red component (0-255) */
  r: number;

  /** Green component (0-255) */
  g: number;

  /** Blue component (0-255) */
  b: number;
};